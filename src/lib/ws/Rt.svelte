<script lang="ts">
	//@ts-nocheck
	import { onMount } from 'svelte';

	// onMount(() => {
	// 	const ws = new WebSocket('ws://localhost:5174');
	// 	if (ws) {
	// 		ws?.onmessage((event) => {
	// 			console.log(event.data);
	// 		});
	// 	}
	// });
</script>
